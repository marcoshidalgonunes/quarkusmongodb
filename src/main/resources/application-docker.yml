quarkus:
  mongodb:
    connection-string: mongodb://mongo:27017

  oidc:
    auth-server-url: http://keycloak:8080/realms/QuarkusRealm
    roles:
      role-claim-path: resource_access/${quarkus.oidc.client-id}/roles

  smallrye-openapi:
    security-scheme: oauth2-implicit
    security-scheme-name: keycloak

    # IMPORTANT: for this Quarkus version, use oauth2-authorization-url (not oauth2-implicit-authorization-url)
    oauth2-authorization-url: http://localhost:8080/realms/QuarkusRealm/protocol/openid-connect/auth

  swagger:
    oauth:
      authorization-url: http://localhost:8080/realms/QuarkusRealm/protocol/openid-connect/auth